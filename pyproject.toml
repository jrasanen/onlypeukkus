[project]
name = "onlypeukkus"
version = "0.1.0"
description = "lists only thumb up emojis"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "imagehash>=4.3.2",
    "pillow>=11.3.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.11",
]
